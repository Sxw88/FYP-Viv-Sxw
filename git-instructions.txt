1) cd to the directory of the project
	cd /home/pi/FYP-Viv-Sxw

2) set your username / email by typing:
	git config user.name "username"
	git config user.email "user email"

3) type command:
	git add -A

4) type command:
	git commit -m "Type short message here"

5) type command:
	git push -u origin master

6) type github account and personal access token.

	Personal Access Token
	i) 	cd to /home/pi
	ii) 	type command: [gpg -d <user>-pat.gpg] to decrypt the GPG file
	iii) 	Access token is printed out, copy and paste :)

7) Check Repository at https://github.com/Sxw88/FYP-Viv-Sxw

